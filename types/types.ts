export type ButtonProps = {
    type: string,
    text: string,
    url?: string
}

export type MainTitleProps = {
    text: string
}

export type AccordionProps = {
    id: string,
    step: string,
    title: string,
}